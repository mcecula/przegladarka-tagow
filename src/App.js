import './App.css';
import AppRoutes from './routes/AppRoutes';
/* const express = require('express');

const app = express();

const cors = require('cors');
app.use(cors()); */


function App() {
  return (
    <div className="App">
      <AppRoutes />
    </div>
  );
}

export default App;
